const express = require('express');
const router = express.Router();
const User = require('../models/user');

router.get('/running'(req, res, next) => {
    // TODO: return only the running entry
})

module.exports = router;
